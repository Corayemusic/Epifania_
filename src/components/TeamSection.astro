---
import { Image } from "astro:assets";
import EfiImage from "~/assets/Efi3.jpeg";
import NellyImage from "~/assets/Nelly.jpg";

const teamMembers = [
  {
    name: 'Psic. Efi Bezi (Argentina)',
    image: EfiImage,
    description: 'Psicóloga especializada en salud emocional.',
    facebook: 'https://facebook.com/efi',
    linkedin: 'https://linkedin.com/in/efi',
    instagram: 'https://instagram.com/efi'
  },
  {
    name: 'Dra. Nelly López Azuz (México)',
    image: NellyImage,
    description: 'Antropóloga con enfoque en salud social.',
    facebook: 'https://facebook.com/nelly',
    linkedin: 'https://linkedin.com/in/nelly',
    instagram: 'https://instagram.com/nelly'
  }
];
---

<section class="py-12">
  <div class="container mx-auto px-4">
    <h2 class="text-center text-4xl font-bold mb-6">¿QUIÉNES SOMOS?</h2>
    <p class="text-center max-w-3xl mx-auto mb-12">
      Somos un colectivo de profesionales dedicados a diversas ramas de la salud emocional, social y organizacional. Brindamos un acompañamiento cercano, profesional e íntegro a las personas.
    </p>
    <div class="flex flex-wrap justify-center gap-4">
      {teamMembers.map((member) => (
        <div key={member.name} class="overflow-hidden rounded-lg shadow-lg text-center max-w-xs mx-auto">
          <Image src={member.image} alt={`Foto de ${member.name}`} class="w-24 h-24 rounded-full mx-auto mt-4 object-cover" />
          <div class="p-4">
            <h3 class="text-xl font-bold mb-2">{member.name}</h3>
            <p class="text-sm">{member.description}</p>
            <div class="mt-4 flex justify-center space-x-4">
              <a href={member.facebook} target="_blank" rel="noopener noreferrer" class="text-blue-600">
                <i class="fab fa-facebook-f"></i>
              </a>
              <a href={member.linkedin} target="_blank" rel="noopener noreferrer" class="text-blue-700">
                <i class="fab fa-linkedin-in"></i>
              </a>
              <a href={member.instagram} target="_blank" rel="noopener noreferrer" class="text-pink-500">
                <i class="fab fa-instagram"></i>
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
