---
import { Image } from "astro:assets";
import astronautImage from '../assets/astronaut.png'; // Ruta de la imagen en la carpeta assets

const title = "SOCIOPSICODRAMA";
const description = `
Conformación, entrenamiento y fortalecimiento de equipos de trabajo multilocales vía remoto y/o presencial. Fortaleciendo los vínculos para promover el bienestar laboral, mejorar el clima organizacional, favoreciendo la comunicación asertiva, la seguridad psicológica y los entornos de confianza. Prevención del burnout y cuidado de los cuidadores. Ayudamos en las estrategias organizacionales para potenciar los talentos y las habilidades blandas y de colaboración en organizaciones autogestivas, centradas en las personas.

Primera sesión de psicoterapia bipersonal sin costo.
`;

const workshop = {
  title: "TALLER DE HERRAMIENTAS CREATIVAS CON PSICODRAMA PARA TANATÓLOG@S:",
  objective: `
    Conocer sobre la historia del psicodrama, sus metodologías y experimentar de manera vivencial, para aprender y expandir los recursos creativos terapéuticos grupales. Para ser utilizados en espacios presenciales o virtuales (Zoom).
  `,
  audience: `
     Tanatólog@s experimentados, terapeutas, psicólog@s, estudiantes de tanatología, logoterapia y psicología, personas que acompañan a otras en sus procesos emocionales o realizan actividades afines.
  `,
  duration: "9 horas (3 horas por sesión semanal)",
  modality: "Opcional híbrido, presencial o vía ZOOM.",
};

const widths = [320, 640, 1280]; // Resoluciones deseadas
const sizes = '(max-width: 768px) 100vw, 50vw'; // Configuración responsiva
---

<section class="space-y-8 px-4 md:px-8">
  <h2 class="text-2xl font-bold text-center text-primary">{title}</h2>
  <p class="text-lg text-center text-offset max-w-prose mx-auto">{description}</p>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
    <!-- Contenedor del texto -->
    <div class="space-y-6 md:pl-6"> <!-- Usamos padding-left para margen interno en pantallas medianas -->
      <h3 class="text-2xl font-semibold text-primary">{workshop.title}</h3>
      <p><span class="font-bold">Objetivo:</span> {workshop.objective}</p>
      <p><span class="font-bold">Dirigido:</span> {workshop.audience}</p>
      <p><span class="font-bold">Duración:</span> {workshop.duration}</p>
      <p><span class="font-bold">Modalidad:</span> {workshop.modality}</p>
    </div>
    <!-- Contenedor de la imagen -->
    <div class="md:pr-6"> <!-- Usamos padding-right para margen interno en pantallas medianas -->
      <Image
        class="h-full w-full object-cover rounded-lg shadow-lg"
        src={astronautImage}
        widths={widths}
        sizes={sizes}
        loading="eager"
        alt="A floating astronaut in a space suit"
      />
    </div>
  </div>
</section>
